# Nerd Downloader — Required Dependencies (Python 3.10+)

# Web app (Nerd Downloader) — this is the primary product.
flask>=3.0.0
yt-dlp>=2026.6.9

# Used by the legacy CLI scripts (download_*.py, start.py).
requests>=2.31.0

# ffmpeg must be installed separately for HD/4K merging + MP3:  brew install ffmpeg
#
# Note: the launcher installs nerd_downloader/requirements.txt, which is a subset
# of this file. This root file is the canonical set for CI / manual setup.
